sources:
  docker:
    type: docker_logs
    exclude_containers:
      - openobserve

sinks:
  openobserve:
    type: http
    inputs:
      - docker
    uri: http://openobserve:5080/api/default/default/_json
    method: post

    auth:
      strategy: basic
      user: vector@collector.com
      password: 1Ym2xdivsfvQ1hJr

    encoding:
      codec: json
      timestamp_format: "rfc3339"

    healthcheck:
      enabled: false
